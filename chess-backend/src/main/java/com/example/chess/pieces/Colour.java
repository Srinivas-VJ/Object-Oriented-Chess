package com.example.chess.pieces;
// this enum is called colour because color was taken by java this is too much pain
// don't ask y i just didn't use a boolean and y i used an enum
public enum Colour {
    BLACK,
    WHITE;
}
